version: '3'
services:
  web:
    restart: always
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    container_name: i2i
    volumes:
      - "./public/uploads:/usr/src/i2i/public/uploads"
      - "./public/system:/usr/src/i2i/public/system"
