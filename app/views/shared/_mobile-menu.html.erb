
<div class="c-mobile-menu <% if current_page?(root_path) %> -homepage <% end %>">
  <div class="wrapper">
    <div class="container">
      <button class="btn-close js-close-menu"></button>

      <nav>
        <ul class="nav-list">
          <li class="nav-item <% if current_page?(data_portal_path) %> -current <% end%>">
            <%= link_to 'Data portal', data_portal_path %>
          </li>
          <li class="nav-item <% if current_page?(news_index_path) %> -current <% end%>">
            <%= link_to 'News', news_index_path %>
          </li>
          <li class="nav-item <% if current_page?(libraries_path) %> -current <% end%>">
            <%= link_to 'Library', libraries_path %>
          </li>
          <li class="nav-item <% if current_page?(community_path) %> -current <% end%>">
            <%= link_to 'Community', community_path %>
          </li>
          <li class="nav-item <% if current_page?(about_path) %> -current <% end%>">
            <%= link_to 'About', about_path %>
          </li>
        </ul>
      </nav>

      <div class="app-logo">
        <a href="/"><img src="i2i-logo.svg" alt="i2i logo"></a>
      </div>
    </div>
  </div>
</div>
