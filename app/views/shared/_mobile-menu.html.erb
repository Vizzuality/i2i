<div class="c-mobile-menu <% if current_page?(root_path) %> -homepage <% end %>">
  <div class="wrapper">
    <div class="container">
      <button class="btn-close js-close-menu"></button>

      <nav>
        <ul class="nav-list">
          <li class="nav-item <% if controller.controller_name == 'data_portal' || controller.controller_name == 'report' %> -current <% end%>">
            <%= link_to 'Data portal', data_portal_path %>
          </li>
          <li class="nav-item <% if current_page?(insights_path) %> -current <% end%>">
            <%= link_to 'Insights', insights_path %>
          </li>
          <li class="nav-item <% if current_page?(initiatives_path) %> -current <% end%>">
            <%= link_to 'Initiatives', initiatives_path %>
          </li>
          <li class="nav-item <% if current_page?(tools_path) %> -current <% end%>">
            <%= link_to 'Tools', tools_path %>
          </li>
          <li class="nav-item <% if current_page?(about_path) %> -current <% end%>">
            <%= link_to 'About', about_path %>
          </li>
          <li class="nav-item <% if current_page?(search_path) %> -current <% end%>">
            <%= link_to 'Search', search_path %>
          </li>

          <% if current_user %>
            <li class="nav-item <% if current_page?(datasets_path) %> -current <% end%>">
              <%= link_to 'Datasets', datasets_path, data: { turbolinks: false } %>
            </li>
            <li class="nav-item <% if current_page?(account_path) %> -current <% end%>">
              <%= link_to 'Account details', account_path, data: { turbolinks: false } %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign out', destroy_user_session_path, method: :delete, data: { turbolinks: false } %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Log in', new_user_session_path, data: { turbolinks: false } %>
            </li>
          <% end %>
        </ul>
      </nav>

      <div class="app-logo">
        <%= link_to image_tag('/i2i-logo.svg', :alt => 'insight2impact'), root_path %>
      </div>
    </div>
  </div>
</div>
