<div class="l-keep-updated">

  <div class="l-header">
    <div class="wrapper">
      <%= render partial: 'shared/header', locals: { isCover: false } %>
    </div>
  </div>

  <div class="l-header-section">
    <div class="wrapper">
      <div class="row">
        <div class="container">
          <div class="grid-s-12 grid-m-6">
            <h2 class="title -dark -big -center">Blog</h2>
            <p class="subtitle">Description text. Lorem ipsum sit amet ornare
              sem lacinia quam venenatis vestibulum.</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="content">
    <div class="wrapper">
      <div class="l-card-grid">
        <div class="row">
          <% @posts.each do |post| %>
            <div class="grid-s-12">
              <article class="c-card -horizontal">
                <div class="picture" style="background-image: url(<%= post.image %>)">
                  <%= link_to '', keep_updated_blog_path(post.id) %>
                </div>
                <div class="metadata">
                  <h4 class="title -dark -xs"><%= post.title %></h4>
                  <% if post.has_attribute? 'date' %>
                    <h4 class="title -sea -small"><%= post.date %></h4>
                  <% end %>
                  <p><%= post.summary %></p>
                </div>
              </article>
            </div>
          <% end %>
          <div class="grid-s-12">
            <div class="grid-footer">
              <div class="grid-s-12 grid-m-4 grid-l-3">
                <button class="c-button -sea js-btn-more">Show more</button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
